<UserControl x:Class="TCP.App.Views.InfoPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{DynamicResource Brush.Background.Primary}">
    <!--
        InfoPanel.xaml - Info panel view
        
        Bu view versiyon bilgilerini ve uygulama hakkında bilgileri gösterir.
        MVVM Pattern: Sadece UI, hiçbir mantık içermez.
        
        Gelecekte buraya info panel içeriği eklenecek.
        Şu an için VersionManager'dan versiyon bilgisi okunabilir (ViewModel üzerinden).
    -->
    <Grid>
        <!-- Şu an boş - gelecekte versiyon bilgileri eklenecek -->
    </Grid>
</UserControl>

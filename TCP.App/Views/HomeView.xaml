<UserControl x:Class="TCP.App.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:TCP.App.ViewModels"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="Transparent">
             
    <UserControl.DataContext>
        <viewmodels:HomeViewModel/>
    </UserControl.DataContext>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Senaryo Yönetimi" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,20"/>

        <!-- Yeni Senaryo Ekleme -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ui:TextBox Text="{Binding NewScenarioName, UpdateSourceTrigger=PropertyChanged}" 
                        PlaceholderText="Yeni Senaryo Adı..." 
                        Margin="0,0,10,0"/>
            <ui:Button Grid.Column="1" 
                       Command="{Binding CreateScenarioCommand}" 
                       Content="Oluştur" 
                       Appearance="Primary"/>
        </Grid>

        <!-- Senaryolar Listesi -->
        <ListView Grid.Row="2" 
                  ItemsSource="{Binding Scenarios}" 
                  Background="Transparent"
                  BorderThickness="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="16"/>
                            <TextBlock Text="{Binding UpdatedAt, StringFormat='Son Güncelleme: {0:g}'}" 
                                       FontSize="12" Opacity="0.7"/>
                        </StackPanel>

                        <ui:Button Grid.Column="1" 
                                   Content="Yükle" 
                                   Command="{Binding DataContext.LoadScenarioCommand, RelativeSource={RelativeSource AncestorType={x:Type ListView}}}" 
                                   CommandParameter="{Binding}"
                                   Margin="10,0,5,0"
                                   Appearance="Secondary"/>

                        <ui:Button Grid.Column="2" 
                                   Content="Sil" 
                                   Command="{Binding DataContext.DeleteScenarioCommand, RelativeSource={RelativeSource AncestorType={x:Type ListView}}}" 
                                   CommandParameter="{Binding}"
                                   Appearance="Danger"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>

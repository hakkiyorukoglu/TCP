<UserControl x:Class="TCP.App.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:TCP.App.ViewModels"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="Transparent">
             
    <UserControl.DataContext>
        <viewmodels:HomeViewModel/>
    </UserControl.DataContext>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Senaryo Yönetimi" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,20"/>

        <!-- Yeni Senaryo Ekleme -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ui:TextBox Text="{Binding NewScenarioName, UpdateSourceTrigger=PropertyChanged}" 
                        PlaceholderText="Yeni Senaryo Adı..." 
                        Margin="0,0,10,0"/>
            <ui:Button Grid.Column="1" 
                       Command="{Binding CreateScenarioCommand}" 
                       Content="Oluştur" 
                       Appearance="Primary"/>
        </Grid>

        <!-- Senaryolar Listesi -->
        <ListView Grid.Row="2" 
                  ItemsSource="{Binding Scenarios}" 
                  Background="Transparent"
                  BorderThickness="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="16"/>
                            <TextBlock Text="{Binding UpdatedAt, StringFormat='Son Güncelleme: {0:g}'}" 
                                       FontSize="12" Opacity="0.7"/>
                        </StackPanel>

                        <ui:Button Grid.Column="1" 
                                   Content="Yükle" 
                                   Command="{Binding DataContext.LoadScenarioCommand, RelativeSource={RelativeSource AncestorType={x:Type ListView}}}" 
                                   CommandParameter="{Binding}"
                                   Margin="10,0,5,0"
                                   Appearance="Secondary"/>

                        <ui:Button Grid.Column="2" 
                                   Content="Sil" 
                                   Command="{Binding DataContext.DeleteScenarioCommand, RelativeSource={RelativeSource AncestorType={x:Type ListView}}}" 
                                   CommandParameter="{Binding}"
                                   Appearance="Danger"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Canlı Ağ İzleme (Live Network Monitoring) -->
        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="Canlı Cihaz Durumları (Ana PC İzleme)" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,15"/>
            
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Modems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource Brush.Surface}" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                                <StackPanel>
                                    <!-- Modem Header -->
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="📶" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Name}" FontWeight="Bold" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="2" Text="{Binding IpAddress}" Foreground="{DynamicResource Brush.Text.Secondary}" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="3" Text="{Binding Status}" Foreground="{DynamicResource Brush.Palette.Orange}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,15,0"/>
                                    </Grid>
                                    
                                    <!-- Stations List -->
                                    <ItemsControl ItemsSource="{Binding Stations}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="30,5,0,5">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="200"/>
                                                        <ColumnDefinition Width="120"/>
                                                        <ColumnDefinition Width="120"/>
                                                        <ColumnDefinition Width="100"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    
                                                    <StackPanel Orientation="Horizontal" Grid.Column="0">
                                                        <TextBlock Text="💻" Margin="0,0,5,0"/>
                                                        <TextBlock Text="{Binding Name}"/>
                                                    </StackPanel>
                                                    <TextBlock Grid.Column="1" Text="{Binding IpAddress}" Foreground="{DynamicResource Brush.Text.Secondary}"/>
                                                    <TextBlock Grid.Column="2" Text="{Binding RouterPort}" Foreground="{DynamicResource Brush.Text.Secondary}"/>
                                                    <TextBlock Grid.Column="3" Text="{Binding Status}" Foreground="{DynamicResource Brush.Palette.Green}" FontWeight="SemiBold"/>
                                                    <TextBlock Grid.Column="4" Text="{Binding LastMessage}" Foreground="{DynamicResource Brush.Text.Secondary}" TextTrimming="CharacterEllipsis"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>

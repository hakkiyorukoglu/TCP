<UserControl x:Class="TCP.App.Views.SimulationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{DynamicResource BackgroundPrimaryBrush}">
    <!--
        SimulationView.xaml - Simulation modülü view
        
        TCP-0.4: Navigation Shell
        Bu view simulation modülü içeriğini gösterir.
        MVVM Pattern: Sadece UI, hiçbir mantık içermez.
    -->
    <Grid>
        <TextBlock Text="Simulation"
                   FontSize="24"
                   FontWeight="Light"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>
    </Grid>
</UserControl>

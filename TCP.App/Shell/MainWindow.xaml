<ui:FluentWindow x:Class="TCP.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:TCP.App"
        xmlns:views="clr-namespace:TCP.App.Views"
        xmlns:components="clr-namespace:TCP.App.Views.Components"
        xmlns:services="clr-namespace:TCP.App.Services"
        xmlns:viewmodels="clr-namespace:TCP.App.ViewModels"
        mc:Ignorable="d"
        Title="TCP" Height="600" Width="1000"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    <Window.DataContext>
        <viewmodels:MainViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:IsNullOrEmptyConverter x:Key="IsNullOrEmptyConverter"/>
        <local:ThicknessConverter x:Key="ThicknessConverter"/>
        <local:CornerRadiusConverter x:Key="CornerRadiusConverter"/>
    </Window.Resources>
    <!--
        MainWindow.xaml - Application Shell
        
        TCP-0.4: Navigation Shell + Stable Startup
        Bu pencere uygulamanın ana shell'idir.
        
        Layout:
        1. TopBar: Sabit yükseklikte üst çubuk (navigation, icons, window buttons)
        2. ContentArea: Ana içerik alanı (View switching için ContentControl)
        
        ÖNEMLİ: 
        - Hardcoded renk kullanılmıyor, tüm renkler ThemeTokens'tan geliyor
        - WindowStyle="None" ile custom chrome kullanıyoruz
        - Navigation basit ve stable (NO complex router)
    -->
    <Grid>
        <Grid.RowDefinitions>
            <!-- TitleBar & TopBar birleşik -->
            <RowDefinition Height="Auto"/>
            <!-- Content area için kalan alan -->
            <RowDefinition Height="*"/>
            <!-- StatusBar SLOT / Terminal Slot -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Arka plan için tam genişlikte Border -->
        <Border Grid.Row="0"
                Background="Transparent"
                BorderBrush="{DynamicResource Brush.Border}"
                BorderThickness="0,0,0,1"
                Height="48"
                IsHitTestVisible="False"/>

        <!-- TopBar ve TitleBar birleştirildi -->
        <ui:TitleBar Grid.Row="0" Title="">
            <ui:TitleBar.Header>
                <Grid Height="48">
                    <Grid.ColumnDefinitions>
                        <!-- Sol: App name + subtitle -->
                        <ColumnDefinition Width="Auto"/>
                        <!-- Orta: Navigation tabs -->
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                
                <!-- Sol: Uygulama adı ve subtitle -->
                <StackPanel Grid.Column="0"
                            Orientation="Vertical"
                            VerticalAlignment="Center"
                            Margin="15,0,20,0">
                    <TextBlock Text="TCP"
                               FontSize="{DynamicResource Font.Heading.Medium}"
                               FontFamily="{DynamicResource Font.Primary}"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource Brush.Text.Primary}"/>
                    <TextBlock Text="Train Control Platform"
                               FontSize="{DynamicResource Font.Caption}"
                               FontFamily="{DynamicResource Font.Primary}"
                               Foreground="{DynamicResource Brush.Text.Secondary}"
                               Margin="0,-2,0,0"/>
                </StackPanel>
                
                <!-- Orta: Navigation tabs -->
                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                    <!-- Home tab -->
                    <TextBlock x:Name="HomeTab"
                               Text="{DynamicResource String.Home}"
                               FontSize="{DynamicResource Font.Body}"
                               FontFamily="{DynamicResource Font.Primary}"
                               Foreground="{DynamicResource Brush.Text.Primary}"
                               VerticalAlignment="Center"
                               Margin="10,0"
                               Padding="8,4"
                               Cursor="Hand"
                               MouseLeftButtonDown="HomeTab_MouseLeftButtonDown">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="{DynamicResource Brush.Accent.Primary}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    
                    <!-- Electronics tab -->
                    <TextBlock x:Name="ElectronicsTab"
                               Text="{DynamicResource String.Electronics}"
                               FontSize="{DynamicResource Font.Body}"
                               FontFamily="{DynamicResource Font.Primary}"
                               Foreground="{DynamicResource Brush.Text.Secondary}"
                               VerticalAlignment="Center"
                               Margin="10,0"
                               Padding="8,4"
                               Cursor="Hand"
                               MouseLeftButtonDown="ElectronicsTab_MouseLeftButtonDown">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="{DynamicResource Brush.Accent.Primary}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    
                    <!-- Editor tab -->
                    <!-- TCP-1.0.2: Editor reintroduced -->
                    <TextBlock x:Name="EditorTab"
                               Text="{DynamicResource String.Editor}"
                               FontSize="{DynamicResource Font.Body}"
                               FontFamily="{DynamicResource Font.Primary}"
                               Foreground="{DynamicResource Brush.Text.Secondary}"
                               VerticalAlignment="Center"
                               Margin="10,0"
                               Padding="8,4"
                               Cursor="Hand"
                               MouseLeftButtonDown="EditorTab_MouseLeftButtonDown">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="{DynamicResource Brush.Accent.Primary}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    
                    <!-- Simulation tab -->
                    <TextBlock x:Name="SimulationTab"
                               Text="Simülasyon"
                               FontSize="{DynamicResource Font.Body}"
                               FontFamily="{DynamicResource Font.Primary}"
                               Foreground="{DynamicResource Brush.Text.Secondary}"
                               VerticalAlignment="Center"
                               Margin="10,0"
                               Padding="8,4"
                               Cursor="Hand"
                               MouseLeftButtonDown="SimulationTab_MouseLeftButtonDown">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="{DynamicResource Brush.Accent.Primary}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Grid>
            </ui:TitleBar.Header>
            
            <ui:TitleBar.TrailingContent>
                <!-- Sağ: Search + Icons + Version -->
                <Grid Margin="0,0,10,0">
                    <Grid.ColumnDefinitions>
                        <!-- Search box -->
                        <ColumnDefinition Width="Auto"/>
                        <!-- Icons + Version -->
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Search Box with Dropdown -->
                    <!-- TCP-0.6.0: Popup overlay - does NOT affect layout -->
                    <Grid Grid.Column="0"
                          Margin="0,0,15,0"
                          VerticalAlignment="Center">
                        <!-- Search TextBox -->
                        <Grid>
                            <Border x:Name="SearchTextBoxBorder"
                                    Width="250"
                                    Background="{DynamicResource Brush.Surface}"
                                    BorderBrush="{DynamicResource Brush.Border}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Padding="8,5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Search input -->
                                    <TextBox Grid.Column="0"
                                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                             GotFocus="SearchTextBox_GotFocus"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Foreground="{DynamicResource Brush.Text.Primary}"
                                             FontSize="{DynamicResource Font.Caption}"
                                             FontFamily="{DynamicResource Font.Primary}"
                                             VerticalContentAlignment="Center"
                                             Padding="0,0,5,0">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="TextBox">
                                                            <Border Background="{TemplateBinding Background}"
                                                                    BorderThickness="0">
                                                                <ScrollViewer x:Name="PART_ContentHost"
                                                                              Margin="{TemplateBinding Padding}"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </TextBox.Style>
                                        <TextBox.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="0"/>
                                            </Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                    
                                    <!-- Magnifier icon -->
                                    <TextBlock Grid.Column="1"
                                               Text="🔍"
                                               FontSize="{DynamicResource Icon.Size.Small}"
                                               FontFamily="{DynamicResource Font.Primary}"
                                               Foreground="{DynamicResource Brush.Text.Secondary}"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            
                            <!-- Placeholder text (when empty) -->
                            <TextBlock Text="{DynamicResource String.Search}"
                                       FontSize="{DynamicResource Font.Caption}"
                                       FontFamily="{DynamicResource Font.Primary}"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                           VerticalAlignment="Center"
                                       Margin="13,0,25,0"
                                       IsHitTestVisible="False">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SearchText, Converter={StaticResource IsNullOrEmptyConverter}}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding SearchText, Converter={StaticResource IsNullOrEmptyConverter}}" Value="False">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                        
                        <!-- Dropdown Autocomplete Panel (Popup - Overlay) -->
                        <!-- TCP-0.6.0: Popup overlay - does NOT affect layout -->
                        <Popup x:Name="SearchDropdownPopup"
                               Placement="Bottom"
                               PlacementTarget="{Binding ElementName=SearchTextBoxBorder}"
                               StaysOpen="False"
                               IsOpen="{Binding IsDropdownVisible, Mode=TwoWay}">
                            <Border Width="250"
                                    Background="{DynamicResource Brush.Surface}"
                                    BorderBrush="{DynamicResource Brush.Border}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    MaxHeight="200">
                                <ListBox ItemsSource="{Binding FilteredSuggestions}"
                                         SelectedItem="{Binding SelectedSearchItem}"
                                         BorderThickness="0"
                                         Background="Transparent"
                                         MaxHeight="200"
                                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                                         SelectionChanged="SearchListBox_SelectionChanged"
                                         KeyDown="SearchListBox_KeyDown">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderThickness="0"
                                                    Padding="10,8"
                                                    Margin="2">
                                                <TextBlock Text="{Binding DisplayText}"
                                                           FontSize="{DynamicResource Font.Caption}"
                                                           FontFamily="{DynamicResource Font.Primary}"
                                                           Foreground="{DynamicResource Brush.Text.Primary}"/>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="Margin" Value="0"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ListBoxItem">
                                                        <Border Background="{TemplateBinding Background}"
                                                                BorderThickness="0"
                                                                Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="{DynamicResource Brush.Border}"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="{DynamicResource Brush.Accent.Primary}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                </ListBox>
                            </Border>
                        </Popup>
                    </Grid>
                    
                    <!-- Icons + Version -->
                    <StackPanel Grid.Column="1"
                                Grid.Row="0"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0,0,10,0">
                    
                        <!-- Settings icon -->
                        <TextBlock Text="⚙"
                                   FontSize="{DynamicResource Icon.Size.Medium}"
                                   FontFamily="{DynamicResource Font.Primary}"
                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"
                                   Cursor="Hand"
                                   ToolTip="{DynamicResource String.Settings}"
                                   MouseLeftButtonDown="SettingsIcon_MouseLeftButtonDown">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource Brush.Accent.Primary}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        
                        <!-- Info icon - TCP-0.9.0-fix1: Info access enabled -->
                        <TextBlock Text="ℹ"
                                   FontSize="{DynamicResource Icon.Size.Medium}"
                                   FontFamily="{DynamicResource Font.Primary}"
                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"
                                   Cursor="Hand"
                                   ToolTip="Info"
                                   MouseLeftButtonDown="InfoIcon_MouseLeftButtonDown">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource Brush.Accent.Primary}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        
                    <!-- Versiyon bilgisi - TCP-0.7.1: Centralized version display -->
                    <!-- TCP-0.9.0: Version text is clickable, opens Info page -->
                    <TextBlock Text="{x:Static services:VersionManager.DisplayVersion}"
                               FontSize="{DynamicResource Font.Caption}"
                               FontFamily="{DynamicResource Font.Primary}"
                               Foreground="{DynamicResource Brush.Text.Secondary}"
                               VerticalAlignment="Center"
                               Margin="0,0,15,0"
                               Cursor="Hand"
                               ToolTip="Click to open Info"
                               MouseLeftButtonDown="VersionText_MouseLeftButtonDown">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="{DynamicResource Brush.Accent.Primary}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    
                    </StackPanel>
                </Grid>
            </ui:TitleBar.TrailingContent>
        </ui:TitleBar>
        
        <!--
            ContentArea - Ana içerik alanı (View switching)
            TCP-0.4: Navigation Shell
            TCP-0.7.2: Layout Standardization - ContentControl for stable layout
            TCP-1.0.2: Default view changed to EditorView
        -->
        <ContentControl x:Name="ContentArea"
                        Grid.Row="1"
                        Background="Transparent">
            <!-- Default view: EditorView -->
            <views:EditorView/>
        </ContentControl>

        <!-- TCP-1.0.3: AutoCAD style Terminal Panel -->
        <!-- Moved after ContentControl and added ZIndex to ensure it's always on top -->
        <components:TerminalPanel x:Name="AppTerminal" Panel.ZIndex="9999">
            <components:TerminalPanel.Style>
                <Style TargetType="components:TerminalPanel">
                    <Setter Property="Grid.Row" Value="2"/>
                    <Setter Property="Grid.RowSpan" Value="1"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding DataContext.IsExpanded, RelativeSource={RelativeSource Self}}" Value="True">
                            <Setter Property="Grid.Row" Value="1"/>
                            <Setter Property="Grid.RowSpan" Value="2"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </components:TerminalPanel.Style>
        </components:TerminalPanel>
        
    </Grid>
</ui:FluentWindow>
